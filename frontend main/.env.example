# Only needed if the backend is NOT reachable at the same origin under /api
# (e.g. it's deployed on a separate host). Leave unset to use the dev proxy
# (see vite.config.ts) or a same-origin reverse proxy in production.
# VITE_API_BASE_URL=https://api.yourdomain.com/api
